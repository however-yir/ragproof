# {{ run.name }} — RAG Evaluation Report

- **Timestamp:** {{ run.timestamp }}
- **Dataset:** `{{ run.dataset }}` ({{ run.sample_count }} samples)

## Aggregate Metrics

| Metric | Value |
|---|---|
{% for name, value in run.aggregate.items() -%}
| {{ name }} | {{ "%.3f"|format(value) }} |
{% endfor %}

## Per-Sample Results

| ID | Question | Metrics | Latency (ms) | Error |
|---|---|---|---|---|
{% for r in run.results -%}
| {{ r.id }} | {{ r.question[:60] }} | {% for k, v in r.metrics.items() %}{% if v is not none %}{{ k }}={{ "%.2f"|format(v) }} {% endif %}{% endfor %} | {{ "%.0f"|format(r.latency_ms) }} | {{ r.error or "-" }} |
{% endfor %}

---
*Generated by [ragproof](https://github.com/however-yir/ragproof)*
